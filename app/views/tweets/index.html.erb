<p><%= notice %></p>

<h1>投稿一覧</h1>

<% @tweets.each do |tweet| %>
  <p>投稿：<%= tweet.content %></p>
  <%= link_to "投稿を編集する", edit_tweet_path(tweet.id) %>
  <%= link_to "投稿を削除する", tweet_path(tweet.id), method: :delete, data: {confirm: '本当に削除していいですか？'} %>
<%  end %>
<br><br>
<%= link_to "新規投稿する", new_tweet_path %>